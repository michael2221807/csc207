<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fall2018.csc2017.slidingtiles">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".StartingActivity" />
        <activity android:name=".GameActivity" />
        <activity android:name="MainClass.SignupActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="MainClass.ScoreActivity" />
        <activity android:name="mineSweeper.MineGameActivity" />
        <activity android:name="MainClass.GameCenterActivity" />
        <activity android:name="connect4game.Connect4SettingActivity" />
        <activity
            android:name="connect4game.Connect4GameActivity"
            android:label="@string/title_activity_connect4__game_"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name="connect4game.Connect4ComputerAIActivity"
            android:label="Connect4_Game_Minimax_Activity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name="MainClass.LoginActivity" />
        <activity android:name="MainClass.ScoreBoardActivity" />
        <activity android:name="mineSweeper.MineSweeperSettingActivity"></activity>
    </application>

</manifest>